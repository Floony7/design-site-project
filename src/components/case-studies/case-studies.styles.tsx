import styled from "styled-components";
import { SectionContainer } from "../../styles/page-layout";

export const CaseStudiesSection = styled(SectionContainer)`
  padding: 6rem 0;
  display: flex;
  justify-content: center;
`;
